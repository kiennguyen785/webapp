<!-- <!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>

<header class="header">
  <a href="/" class="logo">Sports Shop</a>

  <nav>
    <a href="/">Trang chủ</a>
    <a href="/products">Sản phẩm</a>
    <a href="/cart">Giỏ hàng</a>

    <% if (user) { %>
  <div class="user-menu">
    <span><%= user.full_name %></span>

    <div class="dropdown">
      <a href="/profile">Thông tin cá nhân</a>
      <a href="/orders">Lịch sử đặt hàng</a>

      <% if (user.role === 'admin') { %>
        <a href="/admin/users">Quản lý tài khoản</a>
        <a href="/admin/products">Quản lý sản phẩm</a>
      <% } %>

      <% if (user.role === 'seller') { %>
        <a href="/seller/products">Quản lý sản phẩm</a>
      <% } %>

      <a href="/auth/logout">Đăng xuất</a>
    </div>
  </div>
<% } else { %>
      <a href="/auth/login">Đăng nhập</a>
      <a href="/auth/register">Đăng ký</a>
    <% } %>
  </nav>
</header>

<section class="banner">
  <h1>Website bán đồ thể thao</h1>
  <p>Giày, áo, bóng, phụ kiện và nhiều sản phẩm thể thao khác</p>
</section>

<section class="category-box">
  <h2>Danh mục</h2>
  <div class="categories">
    <% categories.forEach(c => { %>
      <a href="/products?category=<%= c.category %>"><%= c.category %></a>
    <% }) %>
  </div>
</section>

<section>
  <h2>Sản phẩm mới</h2>
  <div class="grid">
    <% products.forEach(p => { %>
      <div class="card">
        <a href="/products/<%= p.id %>">
          <img src="<%= p.image_url %>" alt="">
          <h3><%= p.product_name %></h3>
        </a>
        <p><%= p.category %></p>
        <strong><%= Number(p.price).toLocaleString('vi-VN') %> đ</strong>
        <form action="/cart/add/<%= p.id %>" method="POST">
          <button>Thêm vào giỏ</button>
        </form>
      </div>
    <% }) %>
  </div>
</section>

</body>
</html> -->